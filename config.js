const config = {
  challenge: true, // Set to true if you want to enable password protection.
  users: {
    // You can add multiple users by doing username: 'password'.
    unrealprojectdev: "devedition2025",
  },
};

export default config;
